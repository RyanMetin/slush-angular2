{
  "version": "0.0.1",
  "name": "slushy",
  "description": "Angular2 Using TypeScript",
  "dependencies": {
    "angular2": "^2.0.0-alpha.45",
    "systemjs": "^0.19.5"
  },
  "devDependencies": {
    "jasmine": "^2.3.2",
    "karma": "^0.13.14",
    "live-server": "^0.8.2",
    "parallelshell": "^2.0.0",
    "protractor": "^2.5.1",
    "typescript": "^1.6.2"
  },
  "main": "src/boot",
  "scripts": {
    "postinstall": "npm start",
    "compile": "tsc -p src --outDir dist -w",
    "serve": "live-server --open=.",
    "start": "parallelshell \"npm run compile\" \"npm run serve\""
  }
}
